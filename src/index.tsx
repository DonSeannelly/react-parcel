// Be sure to use the 'import * as' syntax for typescript
import * as React from 'react';
import * as ReactDOM from 'react-dom';

// SCSS support is built in, all you have to do is import the file
import './styles.scss';

/**
 * Welcome! To get started, replace the h1 tag below with your own
 * custom React component.
 */
ReactDOM.render(
  <h1>Make something great!</h1>,
  document.getElementById('root')
);